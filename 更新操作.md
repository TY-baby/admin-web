## 更改逻辑
- 1.充值金额消耗逻辑是充值30~200（不包含200）A档，消耗300，B档200~500（不包含500），消耗600，500以上消耗1000
- 2.当前业务录入一个客户包括对应抖音ID充值金额，当用户在A端选择了挡位点击一键投放，则在B端用户列表生成消耗逻辑。修改内容如下：
   - 2.1:B端用户列表取消![img.png](img.png)取消昵称，ID字段展示。录入客户不根据充值金额判断挡位，挡位是在A端用户点击一键投放时选择
   - 2.2：在B端用户列表抖音ID一栏增加投放时间，是根据A端用户点击一键投放记录时间，挡位也是点击一键投放时记录的挡位
   - 2.3：在导出EXcel按钮的时候，无筛选条件，则提示必须先输入查询条件进行导出
   - 2.4：导出的内容![img_1.png](img_1.png)取消备注、添加时间、授权到期、状态、备注、授权类型，挡位、字段
   - 2.5：根据充值金额消耗逻辑，在导出的EXcel表中进行展示。例如：充值金额是1000，用户点击了A档，则在导出的时候随机生成多条数据，6位码和昵称随机生成，多条数据规则按照挡位，A档是30~200，充值金额是1000，消耗的条数，计算规则是1000/300，不用消耗完，肯定有剩余
  
按照以上逻辑修改代码，然后提交git部署